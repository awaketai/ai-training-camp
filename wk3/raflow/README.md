# RAFlow

<div align="center">

**实时语音转写桌面应用**

基于 Tauri v2 + ElevenLabs Scribe v2 API

[![macOS](https://img.shields.io/badge/macOS-11.0+-blue.svg)](https://www.apple.com/macos/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Tauri](https://img.shields.io/badge/Tauri-v2-orange.svg)](https://tauri.app/)

[功能特性](#功能特性) • [快速开始](#快速开始) • [文档](#文档) • [开发](#开发) • [贡献](#贡献)

</div>

---

## 概述

RAFlow 是一个高性能的实时语音转写桌面应用，能够将你的语音实时转换为文字并自动输入到任何应用程序中。类似 Wispr Flow，但完全开源。

### 核心特性

- ⚡️ **超低延迟**: 端到端延迟 < 500ms
- 🎯 **智能注入**: 自动选择最佳文本输入方式
- 📊 **性能监控**: 实时查看系统运行状态
- 🎨 **现代界面**: React + Tailwind CSS
- 🔒 **隐私优先**: 仅在激活时采集音频
- 🚀 **高性能**: Rust 后端，CPU < 10%，内存 < 100MB
- 🛡️ **健壮性**: 完善的错误处理和自动恢复

### 技术栈

**前端**
- React 18
- TypeScript
- Tailwind CSS
- Tauri API

**后端**
- Rust 2021
- Tauri v2
- cpal (音频采集)
- rubato (重采样)
- tokio (异步运行时)
- tokio-tungstenite (WebSocket)

---

## 功能特性

### 1. 实时语音转写

- 支持多种语言（取决于 ElevenLabs API）
- 实时和最终转写结果
- 音频波形可视化
- VAD (语音活动检测)

### 2. 智能文本注入

- **自动策略选择**
  - 键盘模拟：终端、IDE
  - 剪贴板注入：浏览器、文本编辑器
- **广泛兼容**
  - Chrome, Safari, Firefox
  - VS Code, Terminal, iTerm2
  - Notes, TextEdit, Pages
  - Slack, Discord, Notion

### 3. 性能监控

- **音频处理指标**
  - 处理/丢弃数据包
  - 丢包率
  - 处理时间

- **网络通信指标**
  - 消息发送/接收
  - 数据传输量
  - 重连次数

- **系统资源监控**
  - 内存使用
  - CPU 使用
  - 错误统计

- **健康检查**
  - 自动检测异常
  - 性能优化建议

### 4. 权限管理

- 麦克风访问权限
- 辅助功能权限
- 一键请求授权
- 状态实时显示

---

## 快速开始

### 系统要求

- macOS 11.0 (Big Sur) 或更高版本
- Apple Silicon 或 Intel 处理器
- 4GB+ RAM
- 稳定的网络连接
- ElevenLabs API Key

### 安装

#### 从 Release 安装（推荐）

1. 下载最新的 [Release](https://github.com/YOUR_USERNAME/raflow/releases)
2. 打开 `.dmg` 文件
3. 将 RAFlow 拖到 Applications
4. Control+右键点击 RAFlow，选择"打开"

#### 从源码构建

```bash
# 克隆仓库
git clone https://github.com/YOUR_USERNAME/raflow.git
cd raflow/wk3/raflow

# 安装依赖
pnpm install

# 开发模式
pnpm tauri dev

# 构建发布版本
pnpm tauri build
```

### 首次使用

1. **启动应用**
   - 从 Applications 打开 RAFlow

2. **配置权限**
   - 授予麦克风权限
   - 授予辅助功能权限

3. **设置 API Key**
   - 访问 [ElevenLabs Dashboard](https://elevenlabs.io/app/settings/api-keys)
   - 复制 API Key
   - 粘贴到 RAFlow 设置页面

4. **开始转写**
   - 选择音频设备
   - 点击"开始转写"
   - 对着麦克风说话

详细说明请查看 [用户指南](USER_GUIDE.md)。

---

## 文档

- 📖 [用户指南](USER_GUIDE.md) - 完整使用说明
- 🚀 [发布流程](RELEASE.md) - 构建和发布指南
- 📋 [实施计划](../../specs/w3/raflow/0003-implementation-plan.md) - 开发计划
- 🏗️ [架构设计](../../specs/w3/raflow/0002-architecture-design.md) - 技术架构
- 📝 [需求文档](../../specs/w3/raflow/0001-requirements-specification.md) - 功能需求

---

## 开发

### 项目结构

```
raflow/
├── src/                    # React 前端
│   ├── components/        # UI 组件
│   │   ├── Overlay/      # 转写界面
│   │   ├── Settings/     # 设置面板
│   │   ├── Permissions/  # 权限管理
│   │   └── Performance/  # 性能监控
│   ├── hooks/            # React Hooks
│   └── App.tsx           # 主应用
│
├── src-tauri/             # Rust 后端
│   ├── src/
│   │   ├── audio/        # 音频模块
│   │   │   ├── capture.rs    # 音频采集
│   │   │   ├── resample.rs   # 重采样
│   │   │   ├── vad.rs        # VAD 检测
│   │   │   └── buffer.rs     # 音频缓冲
│   │   ├── network/      # 网络模块
│   │   │   ├── websocket.rs  # WebSocket 客户端
│   │   │   ├── protocol.rs   # 协议定义
│   │   │   ├── batch.rs      # 消息批处理
│   │   │   └── retry.rs      # 重试策略
│   │   ├── input/        # 输入模块
│   │   │   ├── injector.rs   # 文本注入
│   │   │   └── window.rs     # 窗口检测
│   │   ├── utils/        # 工具模块
│   │   │   ├── error.rs      # 错误处理
│   │   │   ├── metrics.rs    # 性能指标
│   │   │   └── permissions.rs # 权限管理
│   │   ├── commands.rs   # Tauri 命令
│   │   ├── state.rs      # 应用状态
│   │   └── lib.rs        # 库入口
│   ├── tests/            # 测试
│   │   ├── integration_tests.rs
│   │   └── performance_tests.rs
│   └── Cargo.toml
│
└── scripts/               # 构建脚本
    └── build-release.sh
```

### 开发工作流

```bash
# 安装依赖
cd wk3/raflow
pnpm install

# 启动开发服务器
pnpm tauri dev

# 运行测试
cd src-tauri
cargo test --all

# 代码检查
cargo clippy --all-targets -- -D warnings
cargo fmt --all -- --check

# 构建发布版本
pnpm tauri build
```

### Make 命令

提供了 Makefile 简化常用操作：

```bash
# 查看所有命令
make -C wk3 help

# 开发
make -C wk3 dev

# 测试
make -C wk3 test          # 所有测试
make -C wk3 test-unit     # 单元测试
make -C wk3 test-integration  # 集成测试
make -C wk3 test-performance  # 性能测试

# 代码质量
make -C wk3 check         # Cargo check
make -C wk3 lint          # Linting
make -C wk3 format        # 格式化

# 构建
make -C wk3 build         # Debug 构建
make -C wk3 release       # Release 构建
make -C wk3 dmg           # 创建 DMG

# 清理
make -C wk3 clean
```

### 测试

```bash
# 单元测试
cd wk3/raflow/src-tauri
cargo test --lib

# 集成测试
cargo test --test integration_tests

# 性能测试
cargo test --release --test performance_tests -- --nocapture

# 测试覆盖率
cargo tarpaulin --out Html
```

### 性能基准

目标性能指标：

- **延迟**: < 500ms (端到端)
- **CPU**: < 10% (M1 Pro)
- **内存**: < 100MB
- **丢包率**: < 0.1%
- **音频处理**: > 10x 实时 (480k samples/sec)
- **指标记录**: > 1M ops/sec

运行基准测试验证：

```bash
cargo test --release --test performance_tests -- --nocapture
```

---

## 架构

### 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend (React)                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  ┌────────┐ │
│  │ Settings │  │ Overlay  │  │Permission │  │Perf    │ │
│  └────┬─────┘  └────┬─────┘  └─────┬─────┘  └───┬────┘ │
└───────┼─────────────┼───────────────┼────────────┼──────┘
        │             │               │            │
        │    Tauri IPC (Commands)     │            │
        │             │               │            │
┌───────┴─────────────┴───────────────┴────────────┴──────┐
│                    Backend (Rust)                        │
│  ┌─────────────────────────────────────────────────┐    │
│  │              AppState (Arc<Mutex>)               │    │
│  └─────────────────────────────────────────────────┘    │
│                           │                              │
│     ┌────────────┬────────┴────────┬────────────┐       │
│     │            │                 │            │       │
│  ┌──▼──┐  ┌─────▼─────┐  ┌───────▼───────┐  ┌─▼───┐   │
│  │Audio│  │  Network  │  │     Input     │  │Utils│   │
│  ├─────┤  ├───────────┤  ├───────────────┤  ├─────┤   │
│  │Cap  │  │WebSocket  │  │TextInjector   │  │Error│   │
│  │Resamp│ │Batch      │  │WindowDetect   │  │Metr │   │
│  │VAD  │  │Retry      │  │Strategy       │  │Perm │   │
│  │Buf  │  │Protocol   │  │               │  │     │   │
│  └──┬──┘  └─────┬─────┘  └───────────────┘  └─────┘   │
└─────┼───────────┼──────────────────────────────────────┘
      │           │
      │           │
  ┌───▼───┐   ┌──▼─────────┐
  │Device │   │ElevenLabs  │
  │ (Mic) │   │Scribe API  │
  └───────┘   └────────────┘
```

### 数据流

```
Microphone → Audio Capture → Resampler → VAD
                ↓                           ↓
           Audio Buffer                Voice Detected
                ↓                           ↓
         WebSocket Client ←─────── Message Batcher
                ↓
         ElevenLabs API
                ↓
         Server Messages
                ↓
         ┌──────┴───────┐
         ↓              ↓
  Partial Transcript  Final Transcript
         ↓              ↓
    Update UI      Text Injector
                        ↓
                  Target Application
```

---

## 贡献

欢迎贡献！请遵循以下步骤：

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 贡献指南

- 遵循现有代码风格
- 添加测试覆盖新功能
- 更新相关文档
- 确保所有测试通过
- 提供清晰的提交信息

---

## 路线图

### v1.0.0 (已完成) ✅

- [x] 实时语音转写
- [x] 智能文本注入
- [x] 性能监控
- [x] 权限管理
- [x] 完整测试套件
- [x] 发布流程
- [x] 用户文档

### v1.1.0 (计划中)

- [ ] 全局快捷键
- [ ] 历史记录
- [ ] 多语言支持
- [ ] 自定义主题
- [ ] Windows 支持

### v1.2.0 (计划中)

- [ ] 本地VAD模型
- [ ] 音频预处理
- [ ] 转写结果编辑
- [ ] 云端同步
- [ ] Linux 支持

---

## 常见问题

**Q: 支持哪些操作系统？**
A: 目前仅支持 macOS 11.0+。Windows 和 Linux 支持在计划中。

**Q: 需要付费吗？**
A: RAFlow 本身免费开源，但需要 ElevenLabs API Key，API 使用按量计费。

**Q: 数据安全吗？**
A: 是的。音频通过 HTTPS 加密传输，API Key 存储在系统钥匙串，不保存任何音频文件。

**Q: 可以离线使用吗？**
A: 不可以。转写功能需要连接到 ElevenLabs API。

**Q: 准确度如何？**
A: 取决于麦克风质量、说话清晰度和环境噪音。推荐使用外接麦克风。

更多问题请查看 [用户指南](USER_GUIDE.md) 或提交 [Issue](https://github.com/YOUR_USERNAME/raflow/issues)。

---

## 许可证

本项目采用 MIT 许可证。查看 [LICENSE](LICENSE) 文件了解详情。

---

## 致谢

感谢以下开源项目：

- [Tauri](https://tauri.app/) - 桌面应用框架
- [React](https://react.dev/) - UI 框架
- [cpal](https://github.com/RustAudio/cpal) - 跨平台音频采集
- [rubato](https://github.com/HEnquist/rubato) - 高质量重采样
- [tokio](https://tokio.rs/) - 异步运行时
- [ElevenLabs](https://elevenlabs.io/) - 语音转写 API

特别感谢 [Wispr Flow](https://www.wispr.ai/) 提供的产品灵感。

---

<div align="center">

**Made with ❤️ by RAFlow Team**

[⭐ Star on GitHub](https://github.com/YOUR_USERNAME/raflow) • [🐛 Report Bug](https://github.com/YOUR_USERNAME/raflow/issues) • [💡 Request Feature](https://github.com/YOUR_USERNAME/raflow/issues)

</div>
